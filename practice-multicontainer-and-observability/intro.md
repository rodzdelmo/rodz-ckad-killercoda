# CKAD Practice: Multi-Container Pods & Observability

Three independent recaps:

- tell an init container apart from a long-running one;
- fix a liveness probe that's killing a perfectly healthy app; and
- pick the right `kubectl logs` command for a specific container in a multi-container Pod.

Click **Start** when the terminal is ready.
