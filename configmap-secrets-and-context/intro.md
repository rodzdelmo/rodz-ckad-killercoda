# ConfigMap, Secrets and Context

First, load a ConfigMap into a Pod's environment. Then, copy a working Secret-backed Deployment pattern onto a second deployment. Finally, a pure read-only recap: inspect a Pod's `securityContext`, `nodeSelector`, and node affinity rules without changing anything.

Click **Start** when the terminal is ready.
