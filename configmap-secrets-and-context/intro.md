# ConfigMap, Secrets and Context

First, load a ConfigMap into a Pod's environment. Then, copy a working Secret-backed Deployment pattern onto a second service. Finally, a pure read-only recap: inspect a Pod's `securityContext` and `nodeSelector` without changing anything.

Click **Start** when the terminal is ready.
