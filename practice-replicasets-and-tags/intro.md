# CKAD Practice: Converting a ReplicaSet to a Deployment

Convert a bare ReplicaSet into a Deployment by hand, trace a fresh rollout on that Deployment back to its exact image tag, then roll back a broken rollout.

Click **Start** when the terminal is ready.
