# CKAD Practice: Secrets & Security Context Recap

First, copy a working Secret-backed Deployment pattern onto a second service. Then, a pure read-only recap: inspect a Pod's `securityContext` and `nodeSelector` without changing anything.

Click **Start** when the terminal is ready.
