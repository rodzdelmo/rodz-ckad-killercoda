# CKAD Practice: Startup Commands & ConfigMaps

Two independent repairs:

- a Pod stuck in `CrashLoopBackOff` because of the wrong startup command; and
- a Pod whose environment variable never shows up because it points at the wrong ConfigMap key.

Click **Start** when the terminal is ready.
