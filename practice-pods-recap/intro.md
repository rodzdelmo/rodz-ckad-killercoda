# CKAD Practice: Pods & Namespaces Recap

Two quick, independent warm-ups for tomorrow's exam:

- find a Pod that landed in the wrong namespace and recreate it correctly; and
- fix a Pod manifest that fails to apply because of a YAML syntax mistake.

Neither task depends on the other. Click **Start** when the terminal is ready.
